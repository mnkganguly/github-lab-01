name : shell commands
on : [push]
jobs:
  run-shell-commands:
    runs_on: ubuntu-latest
    steps:
      - name : echo string
        run : echo "Hello All!!"
      - name : multi line commands
        run : |
          node -v
          npm -v